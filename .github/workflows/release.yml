name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  actions: read

jobs:
  smoke-test:
    name: Release smoke test
    runs-on: ubuntu-latest
    steps:
      - name: Print tag
        run: echo "Tag is ${{ github.ref_name }}"
