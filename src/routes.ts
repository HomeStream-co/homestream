// @refresh reset
/**
 * Shim — re-exports from the canonical split files.
 * Route array: src/routeDefinitions.tsx
 * Path types:  src/routeConfig.ts
 */
export { routes } from './routeDefinitions';
export type { Path, Params } from './routeConfig';
