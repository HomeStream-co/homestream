/**
 * CookieBanner — stub for local installs.
 *
 * The original component loaded GoDaddy/wsimg analytics scripts which are
 * irrelevant for a self-hosted local app. This stub renders nothing so any
 * existing import sites compile without changes.
 */
export default function CookieBanner() {
  return null;
}
