// back-to-back.test.ts
// Retired — coverage consolidated into session-store.test.ts,
// config-store.test.ts, and download-duplicate-resume.test.ts.
import { describe, it } from 'vitest';
describe('back-to-back (retired)', () => { it('no-op', () => {}); });
